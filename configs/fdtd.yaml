# FDTD config (skeleton).
# Fill these values either here or in configs/paths.yaml:
# - lumerical_root
# - fdtd_template_fsp
fdtd:
  lumerical_root: ""
  template_fsp: ""
  hide: true
  chunk_size: 8
  max_retries: 2
  timeout_s:
    import: 120
    run: 600
    extract: 120

